package DGU;

import java.awt.*;
import javax.swing.*;
import java.awt.event.*;

public class Grade_2019111679_026 extends JFrame {
    JLabel label; // JLabel 선언

    public Grade_2019111679_026() {
        setTitle("Image button");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        Container c = getContentPane();
        c.setLayout(new BorderLayout()); // BorderLayout 사용

        ImageIcon normal = new ImageIcon("c:\\Q202\\images\\apple.png");
        ImageIcon rollover = new ImageIcon("c:\\Q202\\images\\apple.png");
        ImageIcon pressed = new ImageIcon("c:\\Q202\\images\\appleB.png");

        JButton btn = new JButton("apple", normal);
        btn.setPressedIcon(pressed);
        btn.setRolloverIcon(rollover);

        label = new JLabel("Apple Button Pressed"); // JLabel 초기화
        label.setVisible(false); // 처음엔 JLabel을 보이지 않게 설정

        c.add(btn, BorderLayout.NORTH); // JButton을 NORTH 영역에 추가
        c.add(label, BorderLayout.SOUTH); // JLabel을 SOUTH 영역에 추가

        // 마우스 리스너 추가
        btn.addMouseListener(new MouseAdapter() {
            @Override
            public void mousePressed(MouseEvent e) {
                label.setVisible(true); // 버튼을 누르면 JLabel을 보이게 함
            }

            @Override
            public void mouseReleased(MouseEvent e) {
                label.setVisible(false); // 버튼에서 손을 뗄 때 JLabel을 숨김
            }
        });

        setSize(250, 150);
        setVisible(true);
    }

    public static void main(String[] args) {
        new Grade_2019111679_026();
    }
}
